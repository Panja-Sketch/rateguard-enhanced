{
  "name": "rateguard-firestore-rules-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Firebase Emulator tests proving browser (client SDK) access to Firestore is denied. Nothing here publishes rules.",
  "scripts": {
    "test": "jest --runInBand"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.1",
    "firebase-tools": "^13.35.1",
    "jest": "^29.7.0"
  }
}
